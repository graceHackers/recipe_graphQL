type Recipe {
  id: ID!
  title: String!
  image: String!
}

type Query {
  getRecipes(ingredient: String!): [Recipe]
    @rest(
      endpoint: "https://api.spoonacular.com/recipes/findByIngredients?apiKey=$apikey&ingredients=$ingredient&number=2"
      configuration: "recipe_config"
    )
}
