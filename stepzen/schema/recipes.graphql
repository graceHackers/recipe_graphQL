type Recipe {
  id: ID!
  title: String!
  image: String!
}

type Query {
  getRecipes: [Recipe]
    @rest(
      endpoint: "https://api.spoonacular.com/recipes/findByIngredients?apiKey=$apikey&ingredients=apples,+flour,+sugar&number=2"
      configuration: "recipe_config"
    )
}
